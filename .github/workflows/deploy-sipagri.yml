name: 🚀 Build and Deploy SipAGRI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    # Empêche le déploiement concurrent sur le même serveur
    concurrency: 
      group: production-deploy
      cancel-in-progress: false

    steps:
      # Étape 1: Récupérer le code
      - name: Checkout code
        uses: actions/checkout@v4

      # Étape 2: Configurer Java et Maven
      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      # Étape 3: Builder le projet avec Maven (cela crée le fichier JAR dans target/)
      - name: Build with Maven
        run: mvn -B -X package --file pom.xml
        # L'option -B est pour le mode batch (non interactif)

      # Étape 4: Déployer le JAR sur le serveur Windows via SCP/SSH
      - name: Deploy to Windows Server
        uses: appleboy/scp-action@v0.1.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          source: "target/*.jar"
          target: ${{ secrets.SERVER_JAR_PATH }}
          # 'strip_components' retire le dossier 'target/' du chemin
          strip_components: 1 

      # Étape 5: Redémarrer le service Windows avec NSSM
      - name: Restart Windows Service
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          script: |
            # Arrêter le service NSSM
            nssm stop "${{ secrets.NSSM_SERVICE_NAME }}"
            # Attendre un peu pour être sûr qu'il soit bien arrêté
            Start-Sleep -Seconds 5
            # Redémarrer le service NSSM
            nssm start "${{ secrets.NSSM_SERVICE_NAME }}"
            # Vérifier le statut du service
            nssm status "${{ secrets.NSSM_SERVICE_NAME }}"
